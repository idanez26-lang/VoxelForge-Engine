# VoxelForge Engine — Changelog v0.0.2

## Ajouté

- configuration CMake principale ;
- presets Debug et Release ;
- bibliothèque statique `VoxelForgeCore` ;
- exécutable `VoxelForgeEditor` ;
- système `Application` ;
- système `Logger` ;
- système `Version` ;
- gestion minimale des erreurs fatales ;
- documentation du bootstrap.

## Résultat attendu

Le programme compile et affiche le démarrage du moteur dans une console.
